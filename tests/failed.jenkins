pipeline {
  agent any

  stages {

    stage ('Report tests') {
      steps {
        nunit testResultsPattern: 'custom_files/testResultsFailed.xml', failIfNoResults: true
      }
    }

  }
}